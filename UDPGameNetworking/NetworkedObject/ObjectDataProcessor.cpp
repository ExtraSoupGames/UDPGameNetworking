#include "ObjectDataProcessor.h"

void ObjectDataProcessor::UpdateValues(std::vector<LerpedValue*>* values, NetworkMessage* msg)
{
	//TODO update each value inside by processing contents of network message
}
